# 已确认决策

2026-09-26 D-27 进度：用户分别验收 FE1 登录/大盘和 FE2 合同接入，授权继续下一项；当前补齐 FE3 附件换版，完成后仍须单独验收。不增加付费模型或真实平台授权。

2026-09-25 D-27 进度确认：用户已验收 UI v0.5 交互设计并授权下一项，开始 FE1 正式登录与角色任务大盘。前端整体授权保持有效；每个可独立验收页面单元仍单独报告，真实附件接入/复核/报告/恢复按后续单元接入。

最新确认（2026-09-25，D-27）：用户验收 F6 待办附件恢复，并明确“批准前端开发，而后以你建议的顺序来执行”。调整 D-25 的先后顺序：先核对既有 Pencil 样例并完善可交互 UI，用户确认设计后逐页面实现 React/TypeScript/Vite 客户端并对接真实 API；并行推进的含义为在页面对接过程中收口 A8 操作记录，而非另起未经授权的功能。最后以 F1–F6 / A1–A8 进行本机前后端总体验收。后端证据与标准不取消，设计预览不代替接口或业务验收；目标机验证暂不纳入当前完成条件。每个独立功能继续等待用户验收。已有前端开发授权，无须按旧 D-25 重复索取同一授权。

最新确认（2026-09-25，D-26）：用户“按你的建议来，确认采用此规则”：本地上传任务首次模拟回写时，由法务选择已有模拟审批单并固定绑定；待办导入沿用原绑定。绑定属于任务，跨审查/文档版本保留，失败重试也不得改写目标。当前已有目标为 `demo-f1-001`，不接受任意客户端 ID；不新增真实平台接入。

最新确认（2026-09-25，D-25）：用户验收同版 Markdown/PDF 报告并授权进入下一项模拟审批评论回写，同时明确要求：后端集成验收通过后，必须再询问用户并取得前端开发审批，才能开始前端。此决定替代 D-20 中“不重复询问同一阶段授权”的前端授权口径；Pencil 视觉依据和逐功能验收要求保留。

最新确认（2026-09-25，D-24）：用户对低置信度 OCR 处理回复“按你建议的处理业务规则的方式来”，确定受阻并要求换传清晰附件，不保留部分识别草稿，不允许管理员直接重试此类永久失败。实现初始采用任一行分数 <0.9 即 OCR_LOW_CONFIDENCE/replace_attachment；0.9 是未标定的技术阈值，不代表用户确认识别准确率，也无法保证不漏字。高分结果仍提示法务对照原图。适用于当前图片 OCR 单元，不扩大扫描 PDF、付费调用或后续功能授权。

最新确认（2026-09-25，D-23）：用户针对 F3 CPU OCR 依赖准备方案回复“允许安装”。授权在项目 `.tools/ocr` 准备隔离运行时与必要兼容 Python，在 `storage/ocr` 下载模型与缓存，并继续真实样例识别及坐标验证。此为 D-20 的 OCR 项例外，不修改主 `.venv`、全局 PATH 或已有 Conda 环境，不包含付费模型调用或目标机验证；功能仍逐项验收。

最新确认（2026-09-25，D-22）：针对 DOCX 固定预览缺少 LibreOffice，用户明确回复“允许，请安装”。授权在本机安装 LibreOffice 并继续真实转换验证，作为 D-20 暂不安装依赖的本项例外；不扩大为其他依赖安装、目标机验证或新付费模型调用。优先安装到 D 盘，安装产物和缓存不进入 Git；后续功能仍逐项验收。

最新确认（2026-09-25，D-21）：用户提供官方 CDN 链接 `https://cdn.deepseek.com/api-docs/deepseek_v4_tokenizer.zip`，并明确同意“官方 tokenizer 离线估算、单次预留 0.10 元、输出限制 4096 token、禁止自动重试，返回后按 usage 核算”。0.10 元是应用预算控制值，不是供应商账单绝对上限；沿用项目 50 元总预算与一次合成 F1 真实调用授权，不新增付费次数。此决定替代此前“须证明线上输入 token 上界才可继续”的阻塞条件：本地模板与线上包装可能不同、包内 model_max_length=16384 与页面 1M 不一致均保留为已知限制，不声称验证了线上精确计数。当前已接入有摘要校验的本地估算；费用预计超过 0.10 元拒绝，预计未超限时仍固定预留 0.10 元；未知用量保持占额，已知用量按冻结单价核算，不截断超预留金额。供应商实际金额仍以账单为准。

最新确认（2026-09-24，D-19）：用户明确“上述复核工作已全部核对完成”。已向用户提交的演示内容复核记为通过；不外推为尚未生成的模型建议、未实现的业务流程或专业法务核定通过。

最新安排（2026-09-24，D-20）：用户同意按建议的后续顺序推进，并指定工作区 Pencil 样例为前端 UI 与功能实现依据。已核对实际文件为根目录 `pencil-new.pen`（schema 2.17，10 个 v0.4 画板），不是 `pencil-new/.pen`。先补齐后端业务链并逐功能验收，再实施前端及真实接口联调；此条包含后续前端实施授权，不重复询问同一阶段授权，仍保留每项功能验收。画板决定视觉与交互参考，权限和业务范围仍以 MVP SPEC 为准。安装依赖与目标机验证暂不作为当前工作项；不将未验证项目记为通过，也不据此删除原验收要求。

最新确认（2026-09-24，D-18）：用户表示目前无法找到法务人员，暂由本人负责演示规则、候选法律依据和示范条款的内容复核。记录为用户演示内容复核，不记为专业法务核定；专业核定仍待完成，保留“演示规则／待法务核定”标记。不阻塞其他后端开发，不改变系统既定角色权限或赋予管理员代审权限。

最新确认（2026-09-24，D-17）：用户针对“是否将 MVP_SPEC.md 作为正式需求基线”明确回复“确认”。该文档现为已验收的首期唯一可执行需求基线；其他文档中的待验收基线表述为历史状态。其他设计文档、功能实现、前端开发与法务核定不因此自动通过。

状态：记录截至 2026-09-23 的用户确认选择；具体可执行行为以 [MVP SPEC](MVP_SPEC.md) 为准。本文件记录决策来源与变更，不重复需求全文。原始需求见 [Demand.md](../Demand/Demand.md)，先期设计见 [Demand/SPEC.md](../Demand/SPEC.md)。

| ID | 已确认决定 | 作用 |
| --- | --- | --- |
| D-01 | 首期为可运行演示版，端到端合同类型为中国大陆软件采购；只用合成数据 | 控制首期风险与验收范围 |
| D-02 | 固定 F1–F6 样例、A1–A8 验收；演示规则先做可审版本，大盘/工作台与三角色基本权限纳入首期 | 确定可观察的交付结果 |
| D-03 | 规则编辑页、真实审批平台和其他合同类型后置；模拟回写须明确标识 | 避免把模拟能力写成真实集成 |
| D-04 | “双端分离”指浏览器前端与后端 API 分离，不是按角色做两套客户端；单仓库、单 Windows 电脑运行，最终用一个启动脚本启动 | 确定系统边界 |
| D-05 | 首期先后端、后前端。后端以 API 和固定样例独立跑通完整业务链，前端再接入并完成 UI 及目标机全链路验收 | 确定两个大阶段的通过门槛 |
| D-06 | 目标是在另一台约 8 GB 内存 Windows 电脑验证，可安装依赖，不依赖独立 GPU；在目标机验证 API 直连与 CPU OCR | 限定部署与性能验证环境 |
| D-07 | 前端 React + TypeScript + Vite + PDF.js；后端 FastAPI + Python + SQLite + 本地文件；DOCX/PDF/OCR 候选为 LibreOffice、pdfplumber、PaddleOCR CPU | 固定技术方向，候选解析组件仍需样例验证 |
| D-08 | 解析、OCR、模型审查使用单 FastAPI 服务内的持久任务处理器，状态落 SQLite，重启后可恢复 | 降低单机运行复杂度 |
| D-09 | DeepSeek API 优先用于合成合同；总预算 50 元人民币。预计本次调用会突破上限时硬停止新调用，保留任务；追加额度须用户明确决定 | 约束外部调用；实际供应商账单另行核对 |
| D-10 | 本地任务、法务修改、版本及模拟回写跨重启保留，只能显式重置 | 保留审查证据 |
| D-11 | `docs/MVP_SPEC.md` 在用户验收后成为唯一可执行需求基线；`Demand/` 原文留作来源，根目录 `AGENTS.md` 指向 `docs/` | 解决旧文档与新决定的优先级 |
| D-12 | 2026-09-23 用户确认先补[前端设计](FRONTEND_DESIGN.md)及必要接口契约；仅设计文档，不开发代码；开发仍按 D-05 先后端再前端 | 提前核对页面与 API，不改变阶段验收顺序 |
| D-13 | 2026-09-23 用户明确授权开始后端开发，并要求按逐功能自审、报告和用户验收推进；现有文档仍待单独验收，前端开发仍后置 | 以当前文档为后端工作依据，技术通过不自动进入下一功能 |
| D-14 | 2026-09-23 用户要求每个功能实现后添加可单独运行的冒烟测试，用于核查具体实现和 bug 回归 | 更新 MVP SPEC 第 6 节、工作指令与逐功能验证证据；不改变用户验收门槛 |
| D-15 | 2026-09-24 用户同意本期演示采用 `deepseek-flash`，并授权一次合成 F1 真实验证，要求不要消耗过多额度 | 不等于无限次调用授权；实施保护为本次预计费用不超过 0.10 元，计入既定 50 元预算，超限不发送、失败不自动追加调用；实际账单仍须核对 |
| D-16 | 2026-09-24 用户要求使用本地配置文件，自行填入 API Key，配置文件不得进入推送 | 使用 Git 忽略且未跟踪的 `secrets/deepseek.ini`；标准库加载，不要求全局环境变量，不记录密钥内容 |

先期 SPEC 写有“核心流程设计、技术选型在后续阶段处理”，现已由用户后续确认的 D-04 至 D-11 更新；保留原文不回写，以本轮文档作为待验收的新基线。后续变更应记录决定日期、影响章节和用户确认，再修改 `MVP_SPEC.md`；尚未确认的技术事实或外部能力写入 [实施状态](IMPLEMENTATION_STATUS.md) 的 `UNVERIFIED`，不得伪装为用户决定。
