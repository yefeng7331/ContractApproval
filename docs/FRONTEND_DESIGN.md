# 前端设计与交互契约

## 2026-09-26 A8：管理员任务处理记录

管理员任务详情另列处理记录，读取 `GET /tasks/{id}/processing-records` 的解析、规则、模型和 Markdown/PDF 报告元数据。按文档版本、阶段及尝试顺序展示版本、结果、故障代码与已有时间；全部/指定文档版本筛选，每页 20 条，由服务端筛选和分页。切换任务、刷新或筛选时重读并校验响应任务与版本；401 退出会话，其他错误可手动重读。业务、法务无入口；不显示正文、模型请求/响应或报告内容。原表没有的时间显示“未记录”，不伪造作业开始时间。

## 2026-09-26 A8：管理员任务操作记录

管理员任务详情显示操作记录，读取 `GET /tasks/{id}/audit-events` 的事件元数据，按事件 ID 顺序展示时间、动作、文档版本、操作者用户名和角色。可切换全部/指定文档版本，每页 20 条；筛选和翻页均由服务端完成。响应必须属于当前任务，指定版本时每条事件必须同版；换任务或手动刷新重新读取。401 使会话失效，其他读取错误提示并提供手动重读。业务和法务无操作记录入口，后端仍执行权限校验；不在记录区展示合同原文、评论正文或模型细节。此页面只覆盖已有审计事件，F1 全链路浏览器核验仍需后续验收。

## 2026-09-26 FE8：管理员受阻恢复

FE7 已由用户验收。管理员在受阻任务详情查看当前文档版本、故障代码、受阻原因和当前处理阶段尝试次数。只有 `blocked/admin_retry` 可按当前 `document_version` 提交一次 `POST /tasks/{id}/retry`；`replace_attachment` 指引业务换件，`budget_decision` 等待预算决策，均不出现重试按钮。`mock_pending` 任务额外读取 `GET /tasks/{id}/attachment-attempts`，按同版任务 ID 核对并展示附件获取历史；它与当前处理阶段尝试次数分开展示。409 和不确定响应锁定重试，要求手动刷新并核对，绝不自动重放。前端仅提供入口，后端逐请求鉴权和状态/版本判断；管理员不读取原文或法务草稿。

## 2026-09-26 FE7：模拟回写

FE6 已由用户验收。任务详情对最近确认版本读取 `GET /tasks/{id}/mock-writeback?review_version=N`；业务、法务可查看标清版本的历史确认结果，管理员只显示当前确认版本的状态与故障。法务在未绑定时通过 `GET /mock-writeback-targets` 选择已有模拟审批单，以确认审查版本和目标调用 `POST /tasks/{id}/mock-writeback`。已绑定目标不可更换；失败重试仍提交同版同目标。`writing` 每 5 秒查询状态，`success` 必须同时收到同版评论 ID；未确认或错版停止操作。网络或服务端写入结果不确定时禁止自动再次提交，先手动刷新状态。业务不显示错误/尝试，管理员不显示评论正文也不提交；页面始终提示仅本地模拟。

## 2026-09-26 FE6：已确认报告

FE5 已由用户验收。任务详情使用 `latest_confirmed_version` 指定唯一报告审查版本；当前文档或审查版本更新后仍可查看旧确认报告，但须标注历史版本，绝不将新草稿用于报告请求。对 Markdown/PDF 分别读取 `GET /reports/status?review_version=N`；仅 `ready` 允许携带 Bearer 请求 `GET /reports/{format}?review_version=N`，以纯文本或内嵌 PDF 预览，并支持下载。`pending` 定时查询状态；`failed` 仅法务显示错误代码和 `POST /reports/{format}/retry`，重试正文携带审查版本，POST 不自动重放。业务仅显示文件可用性，不显示内部错误；管理员没有入口，后端负责最终权限与未就绪拒绝。临时 PDF Blob 地址在预览关闭或切换时撤销。

## 2026-09-26 FE5：法务复核与确认

FE4 已由用户验收。法务工作台在同版证据区下方加载当前审查版本；无版本时按完整规则风险列表初始化，建议默认采纳规则。法务逐项调整保留、等级、来源、最终建议与批注，并填写整体批注；选用规则/已有模型建议时保留来源，人工编辑明确标为 `manual`。`PUT /tasks/{id}/review` 携带 `document_version`、`base_review_version` 与完整风险列表，每次保存创建新草稿版本。已有草稿才显示正式结论和 `POST /tasks/{id}/confirm`；确认提交精确审查版本。已确认后修改先创建新草稿，不能改写原确认。

未提交风险/结论在导航、手动刷新与浏览器关闭时提示；编辑中不以后台轮询覆盖表单。409 版本冲突与结果不确定的写请求均停止自动提交，须重新读取并核对。无同版机器规则或模型结果、未完成机器流程时禁用保存。后端逐请求鉴权，页面仅向法务显示编辑区；业务与管理员不以客户端隐藏代替服务端权限。报告查看、模拟回写和完整浏览器总验收为后续单元；本轮交付状态及证据见实施状态。

## 2026-09-26 FE4：法务原文与风险工作台

FE3 用户已验收。FE4 在法务任务详情提供只读双栏：左侧 PDF.js 固定预览、页码、字段/条款/已解析文本，右侧已保存规则风险、摘录、规则与已有模型建议。高亮切换风险/字段/条款；点证据跳第一页可用区域，点 PDF 区域选回对应证据。只有本版锚点与原文 Unicode 摘录吻合、转换预览的段落范围完整且区域合法时绘制。DOCX/图片的 `preview-map` 与原文版本及文本一致、PDF SHA-256 一致后使用；原 PDF 直接使用解析区域。不可定位明确提示，缺少可选机器草稿仍显示原文；权限拒绝或错版证据停止展示。法务依据仍标待核定。本单元不编辑、确认或生成报告；实际固定预览视觉核对待用户验收。

## 2026-09-26 FE2 / FE3 增量

FE1/FE2 已由用户验收。FE2 接入业务上传 multipart `POST /tasks`、模拟待办 GET 与 import；25 MiB/扩展名/申请信息前置校验，后端再次验证内容。登记回执与处理完成分开；网络不确定时先核对列表，无写请求自动重试。

FE3 在任务详情补齐换版：仅 business 且 blocked/replace_attachment 或 completed/confirmed 显示；multipart `POST /tasks/{id}/documents` 携带打开时的 base_document_version，提交前勾选确认创建新版。409 锁定本次表单，返回大盘刷新后重开；网络/5xx 不确定先核对版本。切换任务/版本卸载旧表单并取消请求，取消不承诺服务器回滚。新版本状态以服务端摘要为准，历史确认有明确版本标记。权限仍由服务端执行。FE3 已由用户验收。

## 2026-09-25 FE1：正式登录与任务大盘

UI v0.5 已获用户验收，当前实现位于 `frontend/`，使用已定的 React/TypeScript/Vite。接入 `POST /api/v1/sessions`、`DELETE /api/v1/sessions/current`、`GET /api/v1/tasks`。角色完全来自登录响应，不提供客户端角色切换；会话仅存内存，到期/401/退出清空受保护视图。退出撤销失败时仍清空本页，并明确提示服务端未确认撤销；请求 15 秒超时可重试。

大盘顺序为处理分布、当前角色下一步、带筛选的明细表；任务进度页展示三组状态、同版信息和受阻原因。管理员不显示合同文件名、风险等级/筛选、历史确认内容或法务操作。非管理员正式等级仅展示当前确认版本的返回值；摘要接口的空等级不能区分“未命中规则”和“法务未保留风险”，因此显示“无分级结果（以确认意见为准）”，不冒充法律安全。

当前没有独立统计接口。FE1 每次按 `limit=100` 遍历当前账号可见摘要，按任务 ID 去重后整体发布快照，筛选和每页 20 项显示在客户端完成。概览始终基于未筛选摘要，受阻优先且阶段互斥；这是本机演示规模的最小实现，不承诺多请求期间数据库原子快照，大规模使用需补服务端聚合接口。机器处理中或模拟回写中每 5 秒重新读取；终态停止，用户可手动刷新；离开/退出取消在途请求，错误不展示旧数据为当前结果。

运行与验证入口见 README 最新 FE1 节；`5173` 为正式前端，开发代理固定到 `127.0.0.1:8010`（8000 已被 DailyNews 占用）；旧 `8765` 为 UI 设计附件。本单元不包含上传/复核/报告/重试按钮，不以设计附件中的演示按钮代替 API 功能。依赖安装、TypeScript/Vite 构建和实际 HTTP 代理检查已通过；浏览器自动检查因审批服务 429 未执行，视觉与真实交互待用户手动核对，FE1 为 PENDING_ACCEPTANCE。

手动检查顺序：错误密码 → 业务账号仅本人列表/空态 → 法务账号列表 → 管理员无风险/原文 → 筛选不改变概览 → 任务版本与三组状态 → 退出/刷新回登录 → 后端不可用提示与刷新恢复。该顺序验证 FE1，不替代后续完整 A1–A8 页面验收。

## 2026-09-25 D-27：前端授权与 UI v0.5 设计预览

用户已批准先前端设计与接口对接、后本机总体验收；不再等待后端阶段先独立验收。当前首个单元为 `design/ui-preview/index.html`、`preview.css`、`preview.js`，PENDING_ACCEPTANCE。它是无依赖的设计评审附件，非正式客户端，不取代下方 React + TypeScript + Vite + PDF.js 技术选择。无网络请求、账号采集、持久存储或业务写入。正式页面与真实 API 对接为设计确认后的下一单元。

视觉继承根目录 `pencil-new.pen`（当前 2.17 / v0.4，10 个画板）及其本地渲染图：深绿侧栏与白色工作区；主色 #1D6953、正文 #172B25、次级文字 #53635B、分隔 #D9E2DA、浅底 #EFF2EE、警示红 #9E433A。中文使用 Microsoft YaHei，层级通过标题字号与字重区分，不引入在线字体。保留阶段分布、分离的三组状态与左右证据联动；桌面优先，小窗口允许纵向排列和表格横滚，不扩大移动端验收范围。

预览范围：登录布局/失败样式；三角色大盘与筛选、空态/加载态；接入与附件超时反馈；业务当前进度和旧版正式结果；法务双栏风险选择、意见编辑、未保存离开保护/冲突反馈；确认版本的报告独立状态与模拟回写；管理员下载重试和加密附件不可重试。所有内容为合成示例。预览原文是 HTML 内容示意，不是 PDF.js，也不提供真实页码/坐标验收；风险选择仅验证设计意图，正式 A2 必须用后端固定预览重新核对。

设计验收步骤（不需要账号、密钥或启动后端）：

1. 在仓库根目录运行 `.\.venv\Scripts\python.exe -m http.server 8765 --bind 127.0.0.1 --directory design/ui-preview`，浏览器访问 `http://127.0.0.1:8765/`。如该端口已有本预览服务，无须重复启动。
2. 选择“业务经办人”进入预览。检查大盘、筛选与“合同接入”；查看 A 的当前进度和旧版结果，D 的换件指引。未确认版本不呈现草稿或当前正式等级。
3. 切换“法务审查人”，打开“审查工作台”。点击风险/原文，检查选中项；编辑意见后尝试离开或确认，查看保护提示。底部切换“无法精准定位”“规则未命中”“保存版本冲突”。
4. 打开“交付与回写”，查看 Markdown 可用而 PDF 失败的独立状态；点击预览重试、报告版式和模拟回写反馈。均不生成真实文件/评论。
5. 切换“系统管理员”，打开“受阻恢复”，查看原版本重试和尝试历史；切换“附件加密需换件”，重试应禁用。管理员不显示原文或法务编辑区。
6. 检查整体布局、字号、文案及操作顺序；确认或反馈修改点。设计确认后才开始正式页面/API 单元，最终按下方 A1–A8 的业务标准验收。

验证：`node --check design/ui-preview/preview.js` 与 `node design/ui-preview/smoke.cjs` 通过；脚本在 Node VM 中检查渲染逻辑、角色视图、异常状态、编辑保护及转义，不等于真实浏览器交互测试。静态入口本机 HTTP 200 / UTF-8 正常。自动打开 IAB 时审批服务返回 429 / exceeded retry limit，动作未执行；浏览器视觉、真实 DOM 点击与键盘操作均为 UNVERIFIED，待手动核对。未安装依赖，未调用模型。

2026-09-25 F6 对接增量（仅接口设计，未开发前端）：待办导入 HTTP 201 仅表示任务建立，按返回 machine_status 展示；附件超时为 blocked/admin_retry，未取得附件的 filename/format/sha256 可为 null，显示“附件尚未取得”，不得生成预览地址或假正文。管理员按原 document_version 调用既有 POST `/tasks/{id}/retry`；GET `/tasks/{id}/attachment-attempts` 查看专属附件尝试历史。普通业务/法务无重试和历史入口；业务仍可查看本人受阻状态。重试期间禁止重复触发，409 后重新读取状态。

2026-09-25 模拟回写对接增量（前端未开发）：法务首次回写本地上传任务时，通过 `GET /api/v1/mock-writeback-targets` 选择已有目标；已绑定或待办导入任务显示固定目标，不允许改选。POST `/api/v1/tasks/{task_id}/mock-writeback` 提交 review_version 和 mock_approval_id，收到 writing 后按同一版本 GET 轮询；只在 success 且有 comment_id 时展示成功，failed 可再次 POST 重试。同版重复点击显示已有结果。管理员仅展示状态、故障、尝试，无评论正文；业务仅本人已确认版本最终评论与状态。所有结果标“模拟回写”，历史结果标文档及审查版本。后端集成验收后须按 D-25 再申请前端开发审批。

2026-09-25 报告对接增量（前端未实现）：状态 GET `/api/v1/tasks/{task_id}/reports/status?review_version=N` 的 reports 数组包含两格式各自 state/attempts/generated_at，error 仅法务可见。文件 GET `/reports/{format}?review_version=N` 带 Bearer，预览和下载固定同一 review_version；pending/failed 显示不可用，不沿用旧文件。仅法务且 failed 显示 POST `/reports/{format}/retry`，JSON `{"review_version":N}`；ready/pending 重复请求为 409。“模拟回写”标识不表示回写已完成。

2026-09-25 后端增量：`GET/PUT /tasks/{id}/review` 和 `POST /tasks/{id}/confirm` 已接入，接口参数见 BACKEND_DESIGN 的“法务版本与确认增量”。首次编辑显式提交 base_review_version=null，后续取 GET 返回版本；409 后重新读取供人工核对，不能自动覆盖。采纳 rule/model 建议由服务端冻结原文本，编辑用 manual。业务默认读取最新已确认快照，标题必须显示其 document_version/review_version；当前状态取任务摘要，不能把历史等级填到当前未确认任务。`risk_level` 筛选只匹配当前正式等级。前端尚未实现；报告和回写入口仍等待后续后端能力。

2026-09-25 扫描 PDF 对接：`/document` 的 extraction_method=ocr 与 extraction_warning 同样适用于 PDF。PDF 使用受保护原件 `/document/preview`，直接采用 `/document` 同版本段落/条款 rects，不请求 DOCX/图片专用 preview-map。尚无前端实现证据；OCR 提示和受阻换件交互仍遵循 D-24。

2026-09-25 图片 OCR 契约（前端未实现）：extraction_method=ocr 时显示 warning，提示可能漏字/误识别、须对照原图。preview_available=true 后读取受保护 preview 和 preview-map；mapping_scope=ocr_line_regions 为整行框，不是字段精确框。locatable=false 不绘制精确定位；换版清除旧图与坐标。blocked 的 recovery_action 区分管理员重试与换附件；D-24 低置信度显示换传清晰附件，不展示部分草稿或重试按钮。

2026-09-25 DOCX 预览接口增量（后端已验证，前端尚未实现）：法务通过 `/document/preview` 读取固定 PDF，通过 `/document/preview-map` 读取同文档版本的 page_count、段落/条款/字段映射及摘要。原 `/document` 保留既有解析锚点，不据其 DOCX page_count=null 判定无预览；使用 preview_available 判断登记状态。按 document_version/start/end 对应风险与映射，归一化 rects 只在 locatable=true 时绘制，禁止将段落框冒充字段子串精确框。未准备/转换失败/证据损坏需显示接口错误，不显示上次版本的 PDF 或位置。此条是对接契约，不代表 UI 已交付。

2026-09-24 最新授权（D-20）：用户指定根目录 `../pencil-new.pen` 为 UI 样例，并授权按先后端、后前端的顺序实施。文件已结构核对：schema 2.17、10 个 v0.4 画板。开发时须读取对应画板并核对实际页面；结构可读不等于视觉验收。前端权限、版本与接口约束仍以 MVP SPEC 和后端契约为准；画板不新增业务范围。下方“待用户验收”不否定本次实施授权，也不表示页面已经完成。

状态：待用户验收；设计文档，尚无前端实现。产品范围及 A1–A8 以 [MVP SPEC](MVP_SPEC.md) 为准，接口与权限以 [后端设计](BACKEND_DESIGN.md) 为准，状态转移以 [核心流程](CORE_FLOW.md) 为准。本文件只细化首期浏览器界面；先完成设计，开发仍按后端、前端两个阶段推进。

## 1. 客户端边界与页面

单一 React + TypeScript + Vite 浏览器客户端供三个本地测试角色使用，文档预览使用 PDF.js。首期面向 Windows 桌面浏览器；三角色共用页面与组件，登录后按服务端角色呈现允许的操作。界面隐藏不构成授权，所有数据与动作仍由后端逐接口校验。真实审批平台、规则编辑页、批量操作、外部通知及移动端适配不属于首期验收。

| 页面/区域 | 首期内容与动作 | 数据及角色边界 |
| --- | --- | --- |
| 登录 | 输入本地测试账号；失败与会话失效给出提示；退出清除当前界面数据，刷新后重新登录 | `POST /sessions`、`DELETE /sessions/current`；以服务端角色决定可见操作 |
| 大盘 | 合同名称、申请人、业务类型、金额、创建时间、当前文档的机器/法务/回写三组状态及当前正式综合等级；状态/风险筛选，打开任务 | `GET /tasks`；业务只见本人任务，当前版本未确认时等级为空；旧确认结论另标版本，管理员只见职责范围的状态与故障 |
| 接入 | 上传合成 DOCX/PDF/扫描件或选择固定模拟待办；提交后显示异步进度 | `POST /tasks`、`GET /mock-pending`、`POST /mock-pending/{id}/import`；仅业务经办人；模拟入口标“模拟待办” |
| 工作台左栏 | 同版 PDF 分页预览、页码跳转、合同字段与条款、元数据及风险高亮；点击高亮选中右侧卡片 | `GET /tasks/{task_id}/document` 与 `/document/preview`；法务可看草稿，业务仅看本人已确认版本，管理员不可看原文 |
| 工作台右栏 | 综合摘要、分级风险卡片、原文摘录、规则与依据、条款差异、建议复制；点击卡片跳到左栏 | `GET /tasks/{task_id}/risks`；区分规则、模型与法务意见；未核定依据标“待法务核定” |
| 法务操作 | 采纳或修改等级、建议和批注，确认审查版本；显示确认人、时间和版本 | `PUT /tasks/{task_id}/review`、`POST /tasks/{task_id}/confirm`；仅法务，机器完成且证据可核对才可确认 |
| 报告与模拟回写 | 已确认 Markdown/PDF 分别显示待生成、已生成或失败；已生成可预览/下载，法务可重试失败格式；法务触发模拟回写并查看状态、评论 ID、失败原因和重试 | `GET /tasks/{task_id}/reports/status`、`GET /tasks/{task_id}/reports/{format}`、`POST /tasks/{task_id}/reports/{format}/retry`、`POST/GET /tasks/{task_id}/mock-writeback`；业务仅取本人已确认且已生成的报告；明确标“模拟回写” |
| 受阻恢复 | 展示阻塞阶段、原因、尝试次数及恢复指引；受阻需换附件或上一文档已确认后，业务可上传修订版；暂时故障由管理员重试 | `GET /tasks/{task_id}`、`POST /tasks/{task_id}/documents`（提交 `base_document_version` 与附件）、`POST /tasks/{task_id}/retry`；依服务端状态和角色显示动作 |

大盘和工作台分别显示机器、法务、回写三组状态，不能压成一个“已完成”标签。无已启用规则命中时显示“未发现已启用规则风险”，不显示“法律安全”。受阻任务不显示虚构的等级、建议或定位高亮。

v0.4 仪表盘以当前角色可见的任务为统计范围，按当前文档的主处理阶段互斥归入“处理受阻、机器处理中、待法务复核、法务已确认”；受阻优先归类，避免同一任务重复计数。概览显示各阶段份数与占比，并突出当前角色可以处理的下一步；合同明细继续分别展示机器、法务和模拟回写状态。概览按未筛选的可见任务计算，列表的状态与风险筛选不改变概览口径。旧版已确认结果只在对应版本的历史信息中呈现，不计入当前分布；当前版未确认时不显示正式综合等级。

## 2. 页面行为与版本约束

1. 上传或模拟导入返回 `task_id` 后打开任务；HTTP 接收成功只表示任务已创建，不能显示为审查完成。模拟导入在附件处理阶段超时，仍保留任务及受阻状态。
2. 工作台以 `task_id + document_version + review_version` 识别当前视图。文档与预览带 `document_version`，风险另带可选 `review_version`，报告状态/文件带必填确认 `review_version`；预览、字段、风险、编辑与报告必须属于相应版本。切换任务或文档版本时清除旧高亮和选中项，旧请求结果不得覆盖新视图。大盘三组当前状态只属于当前文档/审查版本：新文档为 `pending/pending/not_written`，已确认结论再编辑的新草稿为法务 `in_review`、回写 `not_written`。业务经办人只使用任务摘要给出的最新已确认版本对查看历史正式结果，不能混入更新的未确认草稿；旧正式结论和旧回写结果须标明所属版本，当前未确认版本的等级保持空值。
3. PDF.js 展示后端固定 PDF 预览：DOCX 使用本版转换文件，文本 PDF 使用本版原件，扫描件使用 OCR 对应的本版预览。可定位字段、条款和风险按锚点中的页码与页面区域绘制高亮。点击风险卡片滚动至首个区域并选中全部区域；点击风险高亮选中对应卡片；多页锚点可有多个区域。前端不重新猜测位置或套用其他样例的页码。
4. `locatable=false`、页面区域缺失或版本不匹配时，不绘制“精准定位”高亮；显示原文摘录、可靠的页/段信息和不能定位的原因，交由法务核对。可定位锚点若与预览不吻合，A2 不通过。
5. 条款差异比较原文摘录与建议条款文本；只有说明性建议时显示建议正文，不伪造替换条款。复制不改变审查版本；法务采纳或编辑通过 `PUT /review` 提交基础审查版本。已确认内容再次修改产生新草稿，新版重新确认前不能生成其报告或回写。
6. 从未确认过的任务，业务视图只显示本人进度、阻塞原因和允许的重新上传动作；不请求或展示草稿、原文预览、建议、等级及报告。存在旧确认快照时，业务可单独查看本人旧版正式结果，但当前未确认内容仍不可见。管理员只见任务状态、故障及允许的重试/模拟回写结果，不进入原文或法务编辑区。直接访问页面链接也遵循相同限制。
7. 报告状态、预览和下载始终指定确认版本；Markdown/PDF 独立显示 `pending/ready/failed`，仅 `ready` 可打开，失败时仅法务见错误和同版重试入口，业务只见暂不可用提示。两格式对应同一确认快照，报告失败不改变“法务已确认”。回写按钮显示目标模拟审批单及确认版本。请求中禁用重复点击；失败可对同一键重试，成功显示已有评论 ID，不能因重复点击产生第二条同版评论。

## 3. API 对接、异步状态与错误反馈

所有业务请求使用 `/api/v1` 和 `Authorization: Bearer <token>`。登录返回的不透明令牌只保存在浏览器内存，不写入 Cookie、`localStorage` 或 `sessionStorage`；刷新页面即清除令牌并回登录页，退出时调用服务端撤销接口。预览 PDF 和报告通过带 Bearer 凭据的请求取得数据，再交给 PDF.js 或下载/预览组件，不使用公开静态路径；切换任务或退出时释放上一份预览数据。客户端不持久化合同正文、风险草稿或报告文件。

任务处于 `pending/parsing/reviewing` 或回写为 `writing` 时读取对应状态并展示进度；进入终态、离开页面或会话失效后停止轮询。列表筛选以服务端返回的可见数据为准。法务编辑期间提示未提交内容，提交中禁用重复确认和回写；版本冲突后重新读取当前版本，由法务核对修改，不能自动覆盖。

| 服务端结果 | 页面处理 |
| --- | --- |
| 401 | 清除受保护视图和预览，返回登录，停止轮询 |
| 403 | 显示无权访问/操作，不展示缓存内容；隐藏按钮仅为辅助 |
| 404 | 显示任务或版本不存在，提供返回大盘入口 |
| 409 | 提示状态或版本变化，重新读取并要求法务核对后再提交；未确认报告/回写不可继续 |
| 409/`REPORT_NOT_READY` | 对已确认但仍在生成或生成失败的格式显示暂不可用；法务按报告状态查看错误并仅对失败格式重试，业务不能重试 |
| 422 | 展示附件或输入无法受理的原因，允许修正后重新提交；不显示为异步 `blocked` |
| 异步 `blocked` | 展示阶段、原因、尝试次数及后端恢复类型；新附件由本人业务账号重新上传，暂时故障由管理员重试，预算阻塞等待用户明确调整预算 |
| 模拟回写 `failed` | 保留确认版本、错误和尝试记录；法务可用同一确认版本与模拟审批单重试 |

界面文案区分“机器草稿完成”“法务已确认”“模拟回写成功”。模型建议无效但规则证据完整时，法务可见“需法务补充建议”；证据不足按 `blocked` 处理。未获法务核定的法律依据不作为正式法律结论展示。

## 4. 前端阶段验收映射

后端先以 API 和 F1–F6 完成其阶段证据：F1 为 DOCX，F2 为不同分页的文本 PDF，F3 为扫描件，F4 为修正风险后的合同，F5 为三类受阻附件，F6 为模拟待办超时及回写失败/重试。以下仅列前端补充的可观察结果。每项都在实际后端 API 上验证，模拟数据与真实平台能力分开记录。

| 验收 | 前端操作及通过条件 |
| --- | --- |
| A1 | 业务账号可上传 F1–F3 创建任务、从模拟待办导入 F1，并查看处理状态；法务在机器完成后可于工作台核对本版字段与条款；提交后不提前宣称完成 |
| A2 | F1–F3 分别用本版预览验证卡片点原文、原文高亮点卡片；页码、区域和选中项与固定标注一致；不能定位的项明示限制 |
| A3 | F1–F3 展示两项高风险、原文和建议，F4 无误报；法务采纳/编辑后可见新版本且与机器内容区分 |
| A4 | 业务从未确认的任务看不到草稿、预览和报告；新版本待审时只可见标明版本的旧确认快照，不见新草稿或当前风险等级；法务可编辑确认；管理员无代审/回写入口；直接请求仍由后端拒绝 |
| A5 | 法务确认后，同版 Markdown/PDF 各自显示生成状态，`ready` 可预览、下载；单格式失败由法务同版重试且确认状态不回退，业务仅得本人已生成报告 |
| A6 | 未确认无回写入口；确认后标“模拟回写”，显示写入、失败、重试、成功和评论 ID；重复提交仍对应同版同一评论 |
| A7 | F5/F6 显示阻塞/失败原因及正确角色的恢复动作；重新上传或重试后状态更新，法务可追溯旧版证据 |
| A8 | F1 在另一台约 8 GB 内存 Windows 电脑走通接入、解析、法务修改确认、报告和模拟回写；记录浏览器、运行环境及实际 API/模型证据 |

首期视觉验收以信息清晰、分页预览可核对、角色与状态不误导为准。候选解析组件、目标机兼容性、真实模型调用和真实平台接口均以实际验证或资料确认记录；本设计文档不等于能力已实现。
