# 后端剩余八个工作块执行清单

2026-09-25 D-27 最新：F6 已获验收；前端开发已明确授权。先 UI 设计确认、再逐页面/API 对接，期间收口 A8 统一操作记录，最后本机前后端总体验收。不再把“后端阶段先通过”作为前端开工阻塞；既有后端证据标准保留。

2026-09-25 最新：F3 标题修复已验收；F6 待办超时持久化、管理员原版本重试、尝试历史、租约中断恢复和并发保护已补齐，PENDING_ACCEPTANCE。专项及接口冒烟 20/20 PASS，证据见 IMPLEMENTATION_STATUS 最新节。本单元验收后收口后端集成与 A8 操作记录；后端阶段验收后再申请前端审批。下方 F6 未实现为之前发现。

2026-09-25 当前：工作块 7 模拟审批评论回写已获用户验收，ACCEPTED。工作块 8 后端集成核验 PARTIAL / 未通过：F1–F5 集成及相关回归 18/18 PASS，F3 标题空白兼容修复待用户复验；发现 F6 待办附件超时未持久登记任务，实际请求 500、无任务可重试，A7 FAIL。下一独立功能须补齐待办超时/管理员重试/重启证据，再收口集成和操作记录核对。详见 [验收矩阵](BACKEND_INTEGRATION_ACCEPTANCE.md)。后端阶段验收后按 D-25 再次申请前端开发审批，覆盖下文历史“无须再次询问”安排。

2026-09-25 本轮：用户“验收通过，请进入下一功能”，扫描 PDF 单元记为 ACCEPTED；复杂版式/真实多页等边界保留，不把工作块 3 泛化为全覆盖完成。第 4 项法务编辑和确认：持久版本、并发拒绝、确认快照、业务历史隔离已实现，全量 130/130、补强后专项 5/5 PASS，PENDING_ACCEPTANCE。验收后下一项为同版 Markdown/PDF 报告。报告、模拟回写与前端未抢先实现；下文此前待验收/不进入法务的描述为历史。

2026-09-25 最新：图片 OCR 业务获用户验收。继续第 3 工作块扫描 PDF：图片/无文本页检测、整份 OCR、同版原 PDF 定位、失败恢复已有实现，PENDING_ACCEPTANCE；全量 124/124、最后定向 4/4 通过；真实单页与模拟混合多页验证见 IMPLEMENTATION_STATUS。工作块仍 PARTIAL，复杂版式/同页混排等未验证，不进入法务编辑。

2026-09-25 当前：用户已验收 OCR 基础并授权业务接入。图片持久作业、同版预览/API、字段/条款/规则及失败恢复已有实现，上轮全量 121/121 通过；D-24 已确认低置信度整份受阻换件，本轮定向 2/2 通过，业务单元 `PENDING_ACCEPTANCE`。真实业务证据仅 F3/F5 PNG，多页 TIFF 为模拟，扫描 PDF 待接入。工作块 3 仍 PARTIAL，未进入法务编辑。下方基础待验收与未接入描述为历史，详见 IMPLEMENTATION_STATUS。

2026-09-25 最新：D-23 授权的隔离 CPU OCR 安装和 F3/F5 真实识别验证已完成，基础单元 `PENDING_ACCEPTANCE`。F3 12 行和原图坐标通过固定样例比对，F5 0 行；详见 IMPLEMENTATION_STATUS 和 README。下方缺依赖/待授权为历史。第 3 工作块仍 `PARTIAL`，待基础单元验收后继续持久 OCR 作业、同版预览/API 及失败恢复，尚未开始法务编辑。

2026-09-25 最新验收：用户确认 DOCX 固定预览与定位通过，状态 `ACCEPTED`。继续第 3 项的 F3 CPU OCR 与同版坐标；依赖核查发现项目及现有另一 Conda 环境均缺 PaddleOCR/PaddlePaddle，当前 `BLOCKED_DEPENDENCY`。待用户单独授权项目隔离 OCR 运行时及模型准备；D-22 的 LibreOffice 安装授权不覆盖本项。工作块 3 仍 PARTIAL，未进入法务编辑。

2026-09-25 最新：第 3 项中的 DOCX 固定预览与定位已完成真实本机转换、独立存储、后台与法务 API 接入，状态 PENDING_ACCEPTANCE。用户反馈 LibreOffice 安装退出码 0，实际 F1/F4 各 1 页、12 段落全部定位，验证见 IMPLEMENTATION_STATUS。第 3 项的 OCR 仍未开始，因此整个工作块仍 PARTIAL；下方安装受阻文字为历史。等待本功能验收后再进入下一功能。

2026-09-25 安装增量：D-22 已授权安装 LibreOffice；官方安装包已下载并通过官方 SHA256 与 Windows 签名校验。实际 msiexec 执行被自动审批服务 429 阻塞、未启动。README 已给出管理员 PowerShell 的本地安装脚本；当前第 3 项仍 PARTIAL，真实转换未验证，不重问安装授权，也不声称依赖已就绪。

2026-09-25 当前推进第 3 项：先做 DOCX 固定预览。独立转换边界/段落映射基础组件 10/10 相关测试通过，转换进程为 mock；本机真实预检缺 LibreOffice，状态 PARTIAL / BLOCKED_DEPENDENCY。遵守暂不安装依赖的决定，待用户提供已有可执行路径或调整安装约束。真实 PDF 排版/视觉定位、不可覆盖预览存储/API 接入尚未完成；OCR 后续单独推进。第 1、2 项验收状态不变。

2026-09-25 最新：第 1、2 项均已获用户验收。第 2 项持久模型作业、免费自动完成、授权隔离、建议保存及法务读取已完成离线验证；用户手动执行真实 F1，反馈 `{"state": "completed", "code": null}` 后明确验收通过，状态 `ACCEPTED`。本次调用授权已使用，供应商账单仍须另行核对。本轮只记录验收，等待下一步指令；下方受阻或待验收文字为历史进度。

## 2026-09-24 用户复核完成后的执行顺序

当前第 1 项进度（2026-09-25）：依据 D-21 完成本地 tokenizer 估算、固定 0.10 元预留与私有 HTTP 边界；`PENDING_ACCEPTANCE`。F1 本地输入 476 token、预计 0.03372 元；价格取用户提供页面的高峰缓存未命中 2/8 元每百万 token。本地模板可能不同于线上计费，用户已接受应用估算口径；不再以绝对输入上界为阻塞条件。HTTP 仍仅模拟验证，没有生产传输调用方或真实调用。验收后进入第 2 项，不提前执行。

用户已确认演示内容复核完成，并授权按既定顺序推进及后续参照 `../pencil-new.pen` 实施前端。以下是顺序安排，不是已实现清单；每个编号可拆为独立功能，逐项验证并等待用户验收。

1. 模型费用预检及 HTTP 传输边界：验证输入估算依据、冻结计费、预算预留、超时及错误处理、响应大小限制；不自动重试付费请求。
2. 模型持久作业与建议保存：同版证据绑定、未知用量占额、重启恢复、失败转人工或受阻、完整机器状态；在费用保护具备后完成一次已授权的合成 F1 真实调用。
3. 解析与定位缺口：DOCX 固定 PDF 预览及页码/区域映射，清晰扫描件 OCR 与同版坐标，模糊/空白/加密失败路径。先核对本机可用依赖，缺失时明确报告，未经授权不安装、不虚报验证。
4. 法务编辑和确认：持久审查版本、并发冲突、确认快照；业务用户仅访问本人已确认版本，隔离当前草稿与历史结果。
5. 同版 Markdown/PDF 报告：生成、下载、权限、单格式失败及重试。
6. 持久模拟回写：确认前拒绝、同版幂等、失败重试、重启恢复；接入 F6 故障源。
7. 后端 F1–F6/A1–A8 对应 API 集成验证；明确真实调用与模拟验证边界。
8. 前端按 Pencil 样例依次实现登录、大盘与接入、双栏工作台、编辑确认、历史版本、报告与模拟回写、受阻恢复；每页直接接入已验收 API，逐项浏览器验证。
9. 本机前后端完整演示及用户验收。依赖安装与目标机验证暂缓安排；缺失依赖涉及的功能仍标受阻，目标机和专业法务验证不计为通过。

当前只推进第 1 项；前端视觉依据已确定，无须再次询问同一授权。实际设计文件含登录、大盘、工作台、历史快照、报告回写、管理员恢复、视觉规范、合同接入、关键状态、操作反馈十个画板。

2026-09-24 最新：工作块 4 的预算账本和模型请求构造/返回校验均已验收，官方模型/人民币价格/JSON 契约已核对。专项 4/4 通过，模型响应为合成，无付费调用。用户已选定 deepseek-flash，授权一次合成 F1 真实验证并要求节省额度；本地配置与离线加载器已准备（待验收），用户自行填入密钥。后续逐项接入输入估算、HTTP 传输及故障边界、模型作业、持久结果和完整机器状态；真实调用前仍须核对账号及价格，按本次预计费用 0.10 元上限保护，不自动追加调用。工作块 4 仍 PARTIAL，法务和报告未开始。

2026-09-24 最新：工作块 3 的自动规则保存与暂时故障管理员重试已验收，完整机器结果仍待模型阶段。工作块 4 新增内部预算账本（待验收），支持固定50元并发预留、冻结价格与用量、结算幂等、未知用量跨重启占额、预算不足任务受阻。仅合成价格测试；尚未接入模型作业，官方价格/输入估算/真实调用/人工调账仍未完成。下一项先核对官方模型计费并设计真实调用边界，再逐功能接入模型处理；不得将本组件记为整个模型预算工作块完成。

2026-09-24 最新：法务持久草稿读取已获用户验收；工作块 3 的本轮增量为服务自动规则保存、尝试持久化、原子失败恢复及暂时故障管理员同版重试，待验收。纯本地规则用短事务独占，不新建运行租约；中断回滚后重启重扫，历史阻塞尝试保留。机器仍 reviewing；模型、完整机器结果、法务审查版本仍未完成，工作块 3 不整体结项。

2026-09-24 增量：同版 PDF 草稿手动持久化已获用户验收。法务读取已保存 DOCX/PDF 规则草稿 API 已实现，独立冒烟 `tests.test_rule_snapshot_api`，本项待用户验收；不结清自动审查、法务确认、报告或回写工作块。读取区分持久草稿与即时评估，提供版本、保存时间及证据摘要；换版前置条件在测试中模拟，未实现法务签批。

更新日期：2026-09-24。用途：供后续任务窗口规划和逐功能执行；**本文件是工作清单，不是新的需求或验收基线**。产品范围与验收以 [MVP SPEC](MVP_SPEC.md) 为准，状态与证据以 [实施状态](IMPLEMENTATION_STATUS.md) 为准；实现细节分别查阅 [核心流程](CORE_FLOW.md)、[后端设计](BACKEND_DESIGN.md)、[前端设计](FRONTEND_DESIGN.md)及[已确认决策](DECISIONS.md)。开始每项工作前重新核对这些文件和当前工作区，不能仅凭本清单的日期判断进度。

## 起点与执行规则

本轮进度：PDF 字段/条款与规则读取已验收；PDF 手动规则草稿持久化已实现、待验收。工作块 3 的自动审查作业与持久结果读取尚未完成。

最新增量：PDF 自动正文解析已验收；PDF 字段/条款与同版规则读取已接入、待验收。仅新解析 PDF 具备结构化证据；旧记录保留，字段精确框、复杂版式及浏览器定位仍待验证。以实施状态新增条目为准。

最新状态：用户已明确验收规则证据修复和 F2 PDF 提取组件。本轮完成 PDF 自动解析与同版正文读取增量、待验收；字段/条款/风险映射与 OCR 尚未完成。旧描述中的待验收及 PDF 未接入状态以实施状态最新记录为准。

- 当前已有账号与权限、接入和换版、持久解析作业、DOCX 基础提取、法务读取解析结果、即时规则草稿 API。用户已验收“同版固定规则草稿持久化”；“固定规则草稿缺陷修复与证据校验”仍为 `PENDING_ACCEPTANCE`，其行为变更部分不因原组件验收自动转为通过。后续状态以 `docs/IMPLEMENTATION_STATUS.md` 为准。
- 八块是**规划粒度**，不是八次不可拆分的交付。每块可拆为可独立验证的小功能；每完成一个小功能，新增或更新独立冒烟测试，覆盖主要成功路径和关键失败或权限路径，运行并记录命令、结果、模拟前置条件；同步更新 `docs/IMPLEMENTATION_STATUS.md` 的证据与验收状态及 `README.md` 的实际可用步骤。接口、流程或已确认决策变化时更新相应设计文档。
- 每次报告按“业务要求 → 实现位置 → 验证证据 → 未验证/受阻 → 待用户验收”给出；技术测试通过后停在 `PENDING_ACCEPTANCE`，等用户明确验收再进入下一项。法务签批、用户验收和技术验证分别记录。遵守 `AGENTS.md` 的操作红线及当前授权范围。
- 可提前准备样例和接口契约；DOCX、文本 PDF、OCR 可在统一锚点契约后分项开发；报告与模拟回写可在确认快照契约稳定后分项开发。集成验收必须等实际链路完成。并行准备不跳过逐功能验收停点。

## 1. 固定样例与预期标注

**交付**：建立 F1–F6 合成文件或故障场景及可复核的答案清单。对可解析文件标注适用的字段、条款、版本、页码、段落、字符区间、风险、等级和建议；对故障场景标注预期状态与恢复动作。F5 覆盖加密 PDF、空文、严重模糊扫描件，F6 覆盖模拟待办超时和回写失败/重复提交。F1/F4 固定 DOCX 与预览前答案清单、F5 空白 DOCX 子场景、F2 三页文本 PDF 与独立页码标注、F3 清晰合成扫描件与原图像素框标注已验收；F5 严重模糊扫描件固定样例已验收；F5 加密 PDF 固定样例已验收；F6 待办附件超时固定故障源已验收；F6 回写故障与重复提交固定故障源已验收。工作块 2 的 F2 原件受保护预览 API 已获用户验收；固定预览区域及全链路证据仍缺，以上样例单元不直接计作对应场景整体通过。

**完成证据**：样例与答案清单可单独读取；核对样例确为合成数据，F1/F4 的规则预期与当前已启用规则一致。后续每块用同一批标注做逐项对照，只有对应能力实际完成后才记录该场景通过。依据：[MVP SPEC 第 6 节](MVP_SPEC.md#6-固定样例与验收)。

## 2. 三类文档解析、固定预览与定位

**交付**：DOCX 固定 PDF 预览和本版页码/区域映射；文本 PDF 提取；扫描件 CPU OCR；受保护的同版预览 API。字段、条款和风险锚点引用同版规范化全文及预览。pdfplumber 已在 F2 固定样例的独立提取组件中验证、待用户验收；LibreOffice 与 PaddleOCR CPU 仍为未验证候选。

**完成证据**：F1–F3 的正文、字段、条款、页码和可定位区域逐项匹配标注；F2 不复用 F1 页码；F3 OCR 文字与坐标人工可核对；无法可靠定位时返回原因并关闭精准高亮。F5 三类异常进入正确的 `blocked` 状态与恢复路径；预览接口验证版本和三角色权限。依据：[后端设计第 1、3 节](BACKEND_DESIGN.md)。

## 3. 自动审查作业与持久机器结果

**前置**：规则证据校验的行为修复仍待用户验收；手动同版草稿持久化已验收。进入此块时还须核对解析证据契约。

**交付**：把同版规则判定接入服务内持久审查作业，分别保存规则来源、证据、审查阶段状态和尝试记录；提供可读取的持久机器草稿，区别于现有 `persisted=false` 即时 `/risks` 响应。完成、受阻、重试、租约恢复和错版拒绝遵守 [核心流程](CORE_FLOW.md) 与 [后端设计](BACKEND_DESIGN.md)。

**完成证据**：F1 两条风险与同版原文对应，F4 不误报；重启、重复领取、来源证据变化、坏证据和越权请求不能产生错版或重复结论。机器完成状态只在需要的审查环节完成后出现，不能把规则草稿当成法务确认结果。

## 4. DeepSeek 辅助建议与预算

**前置**：机器审查结果能保存规则证据，且可区分规则与模型输出。

**交付**：核实并记录实施时的模型 ID、官方计费、汇率和账号条件；按 [MVP SPEC 第 5 节](MVP_SPEC.md#5-界面与技术约束)实现 50 元人民币演示预算的调用前预留、超额阻止、用量与估算账本、超时或未知用量处理。模型只用于合成合同；真实 F1 调用在取得该付费调用所需的明确授权后执行。

**完成证据**：受控响应覆盖正常、超时、无效内容、预算不足及恢复；规则证据与模型建议分别保存，模型不能制造原文锚点；另留一次真实 F1 调用及供应商账单核对状态。应用估算不写成供应商实际收费或精确上限。

## 5. 法务复核、确认与版本权限

**前置**：机器结果可按文档版本读取，证据足以供法务复核。

**交付**：法务采纳或编辑等级、建议和批注，按基础版本生成复核版本并确认正式结论；确认后修改生成新版本，旧确认快照保留。落实确认前隔离、本人业务仅见已确认版本、管理员不得代审、版本冲突拒绝和操作记录。

**完成证据**：F1 法务修改、确认、再编辑及换附件后，当前三组状态与旧版确认结果严格分离；逐接口覆盖未登录、本人业务、其他业务、法务、管理员及过期基础版本。机器未完成或证据不可核对时不得确认。依据：[后端设计第 2、3 节](BACKEND_DESIGN.md)。

## 6. 同版 Markdown/PDF 报告

**前置**：法务确认快照及版本权限已稳定。

**交付**：确认时为 Markdown、PDF 各自登记生成任务；按同一确认快照生成、保存状态并提供受保护的状态与文件接口。单格式失败由法务按原确认版本重试；报告失败不回退法务确认。

**完成证据**：两格式内容与同版正式结论一致；`pending/failed` 文件请求不返回空文件；业务仅能取得本人已确认且 `ready` 的版本；单格式失败、重试、重启和新审查版本均不覆盖旧报告。依据：[MVP SPEC A5](MVP_SPEC.md#6-固定样例与验收)。

## 7. 模拟审批评论回写

**前置**：法务确认版本及目标模拟审批单已明确；可与第 6 块在稳定的确认快照契约后分别开发。

**交付**：仅对已确认版本写入本地模拟评论，保存回写状态、评论 ID、失败原因和尝试；使用 `(task_id, confirmed_review_version, mock_approval_id)` 唯一键确保重复请求和重启后的幂等性。

**完成证据**：确认前拒绝；F6 首次失败后可重试；重复提交最多一条同版评论并返回已有 ID；新确认版本有独立状态，旧评论保留且清楚标明“模拟回写”。依据：[核心流程第 4 节](CORE_FLOW.md)。

## 8. 后端阶段集成验收

**前置**：第 1–7 块所需能力已各自完成技术验证及用户逐功能验收。

**交付**：在实际后端 API 上用 F1–F6 对照答案清单，逐项记录 [MVP SPEC A1–A8 的后端列](MVP_SPEC.md#6-固定样例与验收)：接入与解析、证据定位、风险与法务编辑、角色权限、同版报告、模拟回写、失败恢复、F1 完整链路。记录请求/响应、版本和状态转移、重启恢复、运行电脑及依赖版本、真实模型调用、应用估算及账单核对状态。

**完成证据**：每项标记 `PASS`、`FAIL`、`BLOCKED` 或 `UNVERIFIED` 并链接对应证据；缺项保持未完成。后端 API 阶段通过后仍需用户明确验收；法务对规则与示范条款的确认另行记录。前端双向联动和另一台 Windows 电脑的完整演示属于前端阶段。

## 后续任务窗口的启动方式

把下面这段连同本文件路径交给新窗口，并指定**一项**当前已获授权的工作：

> 请先按 `AGENTS.md` 指定顺序读取核心文档，核对 `docs/IMPLEMENTATION_STATUS.md` 和当前工作区，再查阅 `docs/BACKEND_WORK_BLOCKS.md`。只处理我指定的下一项已授权功能；先列出业务要求、接口/数据契约、完成证据与当前阻塞，再实施、运行独立冒烟、更新状态文档和 README，报告证据后等待我验收。不要把本清单当作新增需求或把技术测试当作用户验收。

前端设计文档和画板已有初稿、仍待审阅；前端开发需按阶段另行授权。后端阶段通过后，以 [前端设计](FRONTEND_DESIGN.md) 接入已验收 API，逐页面验证，最后完成目标 Windows 电脑的 A1–A8 前端证据。
