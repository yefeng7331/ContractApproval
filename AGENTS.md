# 合同审查系统：工作指令

默认用中文沟通；代码、命令、变量名和路径保留英文。结论先行，直接报告真实进度与阻塞。

## 文档入口与权限

1. 开工前依次读 [MVP SPEC](docs/MVP_SPEC.md)、[核心流程](docs/CORE_FLOW.md)、[后端设计](docs/BACKEND_DESIGN.md)、[前端设计](docs/FRONTEND_DESIGN.md)、[已确认决策](docs/DECISIONS.md)和[实施状态](docs/IMPLEMENTATION_STATUS.md)，并核对当前文件。根目录 [README](README.md)只供人阅读与导航。
2. `docs/MVP_SPEC.md` 待用户验收；验收后为唯一可执行需求基线。设计文档细化实现，`docs/DECISIONS.md` 记录已确认变更，`docs/IMPLEMENTATION_STATUS.md` 记录证据与验收，均不能自行扩大需求。`Demand/Demand.md`、`Demand/SPEC.md` 保留供溯源。本文只规定工作方法，不复制产品需求。
3. 文档仍待单独验收；用户已于 2026-09-23 明确授权后端逐功能开发，以现有文档为工作依据，不把开发授权记作文档验收。2026-09-25 按 D-27 已明确授权前端开发：先确认 UI 设计，再逐页面实现/API 对接，同时收口后端 A8，最后本机前后端总体验收。需求或 README 列出能力不等于授权调用付费模型或接入真实平台；正式法律判断也需要相应依据与法务确认。

## 交付与证据

- 先核对业务要求、数据契约、状态与验收，再实现。风险必须可追溯到同版原文、规则或经核准的依据；规则、模型和法务判断分别保留。
- 每完成一个可独立验证的功能，在同一轮工作中更新 `docs/IMPLEMENTATION_STATUS.md` 的证据与验收状态，并同步更新 `README.md` 的实际可用范围和已验证使用步骤；接口、流程或已确认决策发生变化时更新对应设计文档。随后报告“业务要求 → 实现位置 → 验证证据”，区分已验证、模拟验证、未验证和受阻，并等待用户验收后继续下一功能。技术验证不能代替用户验收或法务签批。
- 每项功能实现后新增或更新可单独运行的冒烟测试，覆盖主要成功路径和关键失败/权限路径；运行并记录命令、结果与模拟前置条件，执行口径见 [MVP SPEC 第 6 节](docs/MVP_SPEC.md)。
- 不把候选组件、模拟回写或未核对法律依据描述为已验证的生产能力。只报告本轮变化、证据、阻塞和下一项必要决定；避免重复抄写需求。

## 操作红线

- 不自动 `git commit` 或 `git push`；提交前先展示变更摘要，提交说明用简洁英文。
- 删除文件、目录或 Git 历史；修改 `.env`、密钥、token、证书或 CI/CD 配置；执行 `git push`、`git rebase`、`git reset --hard`、强制推送；公开发布或生产部署——这些操作均先取得用户明确同意。
- 阶段停止条件：只执行当前授权范围；文档通过技术核对不等于用户已验收。后端及前端开发均已授权；依 D-27 先设计确认再页面/API 对接，每项独立功能报告后仍等待用户验收再继续，不重复索取已获前端授权。DeepSeek 调用遵守 [MVP SPEC](docs/MVP_SPEC.md) 的预算与授权约束，不把应用估算当作供应商实际账单。
- 证据不足时标记 `UNVERIFIED`，不推断缺失的工具输出；能从文件或检查得知的事实先查证，需用户决策时一次只问一个关键问题。
